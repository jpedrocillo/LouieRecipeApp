import { UnlessAppDirective } from './unless-app.directive';

describe('UnlessAppDirective', () => {
  it('should create an instance', () => {
    const directive = new UnlessAppDirective();
    expect(directive).toBeTruthy();
  });
});
